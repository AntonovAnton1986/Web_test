Вопрос 1:
a. Сначала в таблицу A
b. Сначала в таблицу B
c. На усмотрение оператора
d. Если связь идентифицирующая, то сначала в таблицу A, а если нет - на усмотрение оператора

Вопрос 2:
a. (1) первичного ключа, (2) потенциальных ключей
b. (1) внешнего ключа, (2) потенциальных ключей
c. (1) суперключа, (2) внешних ключей
d. (1) внешнего ключа, (2) суперключей
e. (1) суперключа, (2) потенциальных ключей

Вопрос 3:
a. любого из потенциальных ключей
b. первичного ключа
c. внешнего ключа
d. ни одного из потенциальных ключей

Вопрос 4:
a. композитных ключей
b. детерминантов функциональных зависимостей
c. потенциальных ключей
d. внешних ключей

Вопрос 5:
a. Экземпляры сущностей
b. Сущности
c. Связи
d. Атрибуты

Вопрос 6:
a. Название предмета
b. Оценка
c. Фамилия
d. Название факультета
e. Экзамен
f. Номер зачетной книжки

Вопрос 7:
a. уникальность любого кортежа отношения
b. наличие у любого отношения первичного ключа
c. для каждого значения внешнего ключа в ссылающемся (дочернем) отношении должен существовать кортеж с таким же значением первичного ключа в отношении, на которое ссылаются (родительском)
d. для каждого значения первичного ключа в ссылающемся (дочернем) отношении должен существовать кортеж с таким же значением внешнего ключа в отношении, на которое ссылаются (родительском)

Вопрос 8:
a. Верно
b. Неверно

Вопрос 9:
a. Определение сущностей, Определение атрибутов, Установление связей между сущностями
b. Определение сущностей, Выбор возможных типов связей, Установление связей между сущностями
c. Определение сущностей, Определение атрибутов, Выбор возможных типов связей, Установление связей между экземплярами сущностей
d. Выбор экземпляров сущностей, Определение атрибутов, Выбор возможных типов связей, Установление связей между экземплярами сущностей

Вопрос 10:
a. DML
b. DDL
c. DCL

Вопрос 11:
a. WHERE
b. GROUP BY
c. HAVING

Вопрос 12:
a. ORDER BY
b. FROM
c. GROUP BY

Вопрос 13:
a. Набор всех записей из всех столбцов таблицы
b. Набор всех записей из всех столбцов таблицы, при этом данные из столбца city в результирующем наборе будут представлены дважды
c. Набор всех записей таблицы из столбца city

Вопрос 14:
a. SELECT Orders.OrderID, Orders.Date, CustId FROM Orders AS Or
b. SELECT Or.OrderID, Or.Date, CustId FROM Orders Or
c. SELECT Orders.OrderID, Orders.[Date], Orders.CustId FROM Orders AS Or

Вопрос 15:
a. SELECT orderid, unitprice AS price, quantity, (unitprice * quantity) AS total FROM Orders WHERE (unitprice * quantity) >200
b. SELECT orderid, unitprice, quantity, (unitprice * quantity) AS total FROM Orders WHERE total>200
c. SELECT orderid, unitprice AS price, quantity, (price * quantity) AS total FROM Orders WHERE (price * quantity) >200

Вопрос 16:
a. SELECT C.id, C.LastName, O.id FROM Customers AS C JOIN Order AS O ON C.id<> O.id
b. SELECT C.id, C.LastName, O.id FROM Customers AS C RIGHT JOIN Order AS O ON C.id=O.id WHERE O.id IS NULL
c. SELECT C.id, C.LastName, O.id FROM Customers AS C LEFT JOIN Order AS O ON C.id= O.id WHERE O.id IS NULL

Вопрос 17:
a. Да, но только на основании равенства значений в соответствующих столбцах
b. Да
c. Нет

Вопрос 18:
a. SELECT ordId, custId, custName FROM orders JOIN customers ON custId=custId
b. SELECT ordId, custId, custName FROM orders JOIN customers ON c.custId=o.custId
c. SELECT ordId, c.custId, custName FROM orders AS o JOIN customers AS c ON c.custId=o.custId

Вопрос 19:
a. LIKE
b. IS
c. ANY
d. IN

Вопрос 20:
a. LIKE
b. IS
c. BETWEEN... AND ...
d. IN

Вопрос 21:
a. Да
b. Нет
c. Правое граничное значение включается, а левое - нет
d. Левое граничное значение включается, а правое - нет

Вопрос 22:
a. LIKE
b. IS
c. BETWEEN... AND ...
d. IN

Вопрос 23:
a. OFFSET-FETCH зависит от предложения ORDER BY
b. OFFSET-FETCH не зависит от предложения ORDER BY, а зависит только от предложения WHERE
c. ORDER BY зависит от предложения OFFSET-FETCH

Вопрос 24:
a. NULL
b. Значение из столбца LastName
c. Ошибку
d. 0
e. None
f. FALSE

Вопрос 25:
a. Произойдет ошибка и выполнение оператора будет отменено
b. В столбце будет сохранена строка, усеченная до пяти знаков
c. Размер столбца будет автоматически изменен, чтобы вся строка могла быть сохранена

Вопрос 26:
a. Произойдет ошибка и выполнение оператора будет отменено
b. В столбце будет сохранен 0
c. Тип столбца будет автоматически изменен для возможности хранения строковых значений

Вопрос 27:
a. INSERT ... VALUES
b. TABLE... INSERT
c. INSERT ... OUTPUT
d. UPDATE TABLE

Вопрос 28:
a. UPDATE ... SET
b. ALTER TABLE ... SET
c. TRUNCATE ... SET
d. INSERT ... SET

Вопрос 29:
a. DELETE
b. TRUNCATE
c. DROP

Вопрос 30:
a. 100
b. 50
c. 0

Вопрос 31:
a. 50
b. 100
c. 0

Вопрос 32:
a. Ошибку
b. Количество заказов, оформленных каждым сотрудником
c. Количество сотрудников, оформивших заказы

Вопрос 33:
a. SELECT AVG(COALESCE(TotalCost,0)) AS AvgCost FROM Orders
b. SELECT AVG(TotalCost) AS AvgCost FROM Orders
c. SELECT COALESCE(AVG(TotalCost),0) AS AvgCost FROM Orders

Вопрос 34:
a. SELECT COUNT(DISTINCT city) AS UniqCity, COUNT(city) AS TotalAddress FROM Address
b. SELECT COUNT(city) AS UniqCity, COUNT(*) AS TotalAddress FROM Address
c. SELECT DISTINCT COUNT(city) AS UniqCity, COUNT(*) AS TotalAddress FROM Address

Вопрос 35:
a. http://
b. 80
c. /path/to/myfile.html
d. ?key1=value1&key2=value2

Вопрос 36:
a. http://
b. 80
c. /path/to/myfile.html
d. ?key1=value1&key2=value2

Вопрос 37:
a. ... набор модулей, библиотек и фреймворков, которые позволяют разрабатывать веб-приложения.
b. ... это ряд специальных URL, которые в качестве ответа возвращают данные, например в формате JSON.

Вопрос 38:
a. 1-C, 2-D, 3-A, 4-B
b. 1-A, 2-С, 3-D, 4-B
c. 1-D, 2-B, 3-C, 4-A
d. 1-B, 2-A, 3-C, 4-D

Вопрос 39:
a. ... не меняет состояние сервера. Другими словами, безопасный метод проводит операции "только чтение" (read-only).
b. ... обеспечивает доступ к ресурсам только тем пользователям, которые прошли процедуру авторизации.

Вопрос 40:
a. POST
b. GET
c. PUT
d. DELETE

Вопрос 41:
a. ... повторный идентичный запрос, сделанный один или несколько раз подряд, имеет каждый раз разный эффект, изменяющий состояние сервера.
b. ... повторный идентичный запрос, сделанный один или несколько раз подряд, имеет один и тот же эффект, не изменяющий состояние сервера.

Вопрос 42:
a. POST
b. GET
c. PUT
d. DELETE

Вопрос 43:
a. Да
b. Нет

Вопрос 44:
a. ... управления данными
b. ... отображения шаблонов
c. ... cвязи между моделями и шаблонами

Вопрос 45:
a. MVT
b. ORM
c. MVC
d. API

Вопрос 46:
a. auto_field
b. primary_key
c. id

Вопрос 47:
a. migrate
b. makemigrations
c. runserver

Вопрос 48:
a. migrate
b. makemigrations
c. runserver

Вопрос 49:
a. Нет
b. Да

Вопрос 50:
a. Нет
b. Да

Вопрос 51:
a. иногда выполняются
b. выполняются только при непосредственной потребности в данных
c. выполняются сразу после формирования запроса
d. при наличии реляционных связей с другими моделями (например, ForeignKey или ManyToMany) по умолчанию сразу не извлекают данные о связанных объектах.

Вопрос 52:
a. select_related
b. prefetch_related

Вопрос 53:
a. select_related
b. prefetch_related

Вопрос 54:
a. ForeignKey (Many-to-One)
b. ManyToMany (Many-to-Many)
c. OneToOne (частный случай ForeignKey)
d. Связь обратная ForeignKey (One-to-Many)

Вопрос 55:
a. ForeignKey (Many-to-One)
b. ManyToMany (Many-to-Many)
c. OneToOne (частный случай ForeignKey)
d. Связь обратная ForeignKey (One-to-Many)

Вопрос 56:
a. в QMainWindow реализован menuBar
b. в QMainWindow реализован treeViewBar
c. в QMainWindow реализован statusBar
d. нет различий

Вопрос 57:
a. Да
b. Нет

Вопрос 58:
a. QtGui
b. QtCore
c. QtWidgets
d. QtXml

Вопрос 59:
a. Передача данных между потоками.
b. Виджет для отображение данных.
c. Виджет для ввода данных.
d. Обработка основного цикла событий

Вопрос 60:
a. Сигналы и слоты используются для связи между объектами
b. Сигналы это виджеты для отображения данных
c. Слот вызывается, когда излучается связанный с ним сигнал
d. Слот --- это принимающая функция, используемая для получения информации об изменениях состояния в других виджетах

Вопрос 61:
a. Нет
b. Да

Вопрос 62:
a. Для возможности отображать в нём данные
b. Для возможности одного экземпляра QObject отслеживать события, предназначенные для другого экземпляра QObject до того, как последний получит их
c. Для возможности выбора цвета для виджета
d. Для возможности управления одним потоком внутри программы

Вопрос 63:
a. Делегат
b. Представление
c. Модель
d. Виджет

Вопрос 64:
a. С помощью делегата
b. С помощью сигнала
c. С помощью потока

Вопрос 65:
a. Для проектирования
b. Для сортировки
c. Для фильтрации
d. Для удаления

Вопрос 66:
a. могут использоваться для управления режимами отображения или редактирования представлений
b. могут использоваться для управления режимами отображения или редактирования моделей
c. могут использоваться для управления режимами отображения или редактирования слотов
d. могут использоваться для управления режимами отображения или редактирования таблиц

Вопрос 67:
a. Нет
b. Да

Вопрос 68:
a. QThread управляет одним виджетом управления внутри программы
b. QThread управляет одним слотом управления внутри программы
c. QThread управляет одним потоком управления внутри программы
d. QThread управляет одним делегатом управления внутри программы

Вопрос 69:
a. QSettings
b. QColor
c. QTranslator
d. QObject

Вопрос 70:
a. С помощью класса Slot
b. С помощью класса Sender
c. С помощью класса Signal
d. С помощью класса Setter

Вопрос 71:
a. QPushButton
b. QTextBrowser
c. QWebEngineView
d. QWebViewer

Вопрос 72:
a. lupdate
b. lrelease
c. uic
d. rcc

Вопрос 73:
a. Да
b. Нет

Вопрос 74:
a. QSettings
b. QTranslator
c. QResource
d. QThread

Вопрос 75:
a. QSettings
b. QTranslator
c. QResource
d. QThread

Вопрос 76:
a. Стек
b. Односвязный список
c. Очередь с приоритетами
d. Двусвязный список

Вопрос 77:
a. FIFO (first in - first out) - первый пришел - первый ушел
b. LIFO (last in - first out) - последний пришел - первый ушел
c. FILO (first in - last out) - первый пришел - последний ушел

Вопрос 78:
a. FIFO (first in - first out) - первый пришел - первый ушел
b. LIFO (last in - first out) - последний пришел - первый ушел
c. FILO (first in - last out) - первый пришел - последний ушел

Вопрос 79:
a. квадратичная (O(N^2))
b. логарифмическая (O(log(N))
c. линейная (O(N))
d. константа (O(1))

Вопрос 80:
a. Сортировка в убывающем порядке и взятие первого элемента
b. Линейный поиск (прямой перебор)
c. Сортировка в возрастающем порядке и взятие последнего элемента

Вопрос 81:
a. Можно, но не обязательно
b. Не нужно
c. Да, обязательно

Вопрос 82:
a. O(N^2) < O(N) < O(log(N) < O(1)
b. O(1) < O(log(N) < O(N) < O(N^2)
c. O(1) < O(N) < O(log(N) < O(N^2)
d. O(1) < O(N) < O(N^2) < O(log(N)

Вопрос 83:
a. квадратичная (O(N^2))
b. логарифмическая (O(log(N))
c. линейная (O(N))
d. константа (O(1))

Вопрос 84:
a. квадратичная (O(N^2))
b. логарифмическая (O(log(N))
c. линейная (O(N))
d. константа (O(1))

Вопрос 85:
a. квадратичная (O(N^2))
b. логарифмическая (O(log(N))
c. линейная (O(N))
d. константа (O(1))

Вопрос 86:
a. O(N) * O(N) -> O(N^2)
b. O(N) + O(N) -> O(2*N)
c. O(N) + O(N) -> O(N)
d. O(1)

Вопрос 87:
a. O(N) * O(N) -> O(N^2)
b. O(N) + O(N) -> O(2*N)
c. O(N) + O(N) -> O(N)
d. O(1)

Вопрос 88:
a. Обход в глубину
b. Обход в ширину
c. Случайный обход

Вопрос 89:
a. Обход в глубину
b. Обход в ширину
c. Случайный обход

Вопрос 90:
a. O(N^2)
b. O(N)
c. O(N * log(N))
d. O(1)

Вопрос 91:
a. O(N^2)
b. O(N)
c. O(N * log(N))
d. O(1)

Вопрос 92:
a. O(N^2)
b. O(N)
c. O(N * log(N))
d. O(1)

Вопрос 93:
a. O(N^2)
b. O(N)
c. O(N * log(N))
d. O(1)

Вопрос 94:
a. не меняет порядок объектов с одинаковыми ключами сортировки
b. гарантированно отсортирует все значения
c. меняет порядок объектов с одинаковыми ключами сортировки

Вопрос 95:
a. Да, но такие алгоритмы основаны не на сравнении.
b. Нет, быстрее отсортировать никак нельзя

Вопрос 96:
a. __make__
b. __new__
c. __build__
d. __init__

Вопрос 97:
a. Да, в каждом классе обязан присутствовать конструктор
b. Нет, но в большинстве случаев без него не обойтись

Вопрос 98:
a. Да. Если будет название отличное от self, то при запуске программы будет ошибка, что такая переменная не найдена.
b. Нет. self - это просто соглашение об именовании переменных в Python

Вопрос 99:
a. В данном коде нет ни синтаксических ни логических ошибок при оформлении класса
b. В данном коде синтаксическая ошибка. Атрибуты нельзя определять вне конструктора __init__
c. В данном коде логическая ошибка. Атрибуты можно определять вне конструктора __init__, но не желательно. Так как все атрибуты сразу должны появиться после инициализации объекта

Вопрос 100:
a. Атрибуты экземпляра класса
b. Атрибуты класса
c. Статические атрибуты
d. Глобальные атрибуты
e. Локальные атрибуты

Вопрос 101:
a. Глобальные методы
b. Локальные методы
c. Методы экземпляра класса
d. Методы класса
e. Статические методы

Вопрос 102:
a. SomeClass
b. some_attr_1
c. some_attr_2
d. some_method_1
e. some_method_2
f. some_method_3

Вопрос 103:
a. SomeClass
b. some_attr_1
c. some_attr_2
d. some_method_1
e. some_method_2
f. some_method_3

Вопрос 104:
a. Инкапсуляция
b. Наследование
c. Полиморфизм

Вопрос 105:
a. Инкапсуляция
b. Наследование
c. Полиморфизм

Вопрос 106:
a. Инкапсуляция
b. Наследование
c. Полиморфизм

Вопрос 107:
a. Нет
b. Да

Вопрос 108:
a. self.some_attr
b. self.some_attr_
c. self._some_attr
d. self.some_attr__
e. self.__some_attr

Вопрос 109:
a. some_attr
b. Ошибка AttributeError. Доступ к атрибутам такого типа запрещен

Вопрос 110:
a. some_attr
b. Ошибка AttributeError. Доступ к атрибутам такого типа запрещен

Вопрос 111:
a. SomeClass - является базовым или родительским классом по отношению к InheritanceClass. InheritanceClass - является дочерним или производным классом по отношению к SomeClass
b. SomeClass - является дочерним или производным классом по отношению к InheritanceClass. InheritanceClass - является базовым или родительским классом классом по отношению к SomeClass

Вопрос 112:
a. Синтаксическая ошибка. В классе InheritanceClass нет метода __init__
b. value. Атрибут some_attr будет унаследован из родительского класса
c. Ошибка, в классе наследнике нет атрибута some_attr

Вопрос 113:
a. Синтаксическая ошибка, так названия методов при наследовании повторяться не могут
b. 5. Вызовется метод some_method из родительского класса
c. 10. Вызовется метод some_method из дочернего класса

Вопрос 114:
a. next
b. super
c. iter

Вопрос 115:
a. Синтаксическая ошибка, так названия методов при наследовании повторяться не могут.
b. 5. Вызовется метод some_method из родительского класса
c. 10. Вызовется метод some_method из дочернего класса
d. 5 и 10. Вызовется метод some_method из родительского класса, а затем метод some_method из дочернего класса

Вопрос 116:
a. TypeError
b. StopIter
c. i is out of range of argument

Вопрос 117:
a. Int
b. Float
c. List
d. Str
e. Tuple

Вопрос 118:
a. Анонимная функция содержащая одно выражение
b. Функция-генератор
c. Функция вычисления длины волны Де-Бройля

Вопрос 119:
a. Последний yield никогда не будет выведен
b. second_gen является функцией-генератора
c. second_gen является обычной функцией
d. Выведен может быть только первый yield

Вопрос 120:
a. counter(some_func)
b. some_func(counter)
c. def counter ():
d. def some_func()

Вопрос 121:
a. Функция wrapper является декоратором
b. Count внутри функции wrapper не имеет начального значения
c. Count внутри функции wrapper имеет начальное значение равное 1
d. Count внутри функции wrapper имеет начальное значение равное 7

Вопрос 122:
a. Файл filename откроется на чтение
b. Файл filename откроется на запись
c. После завершения работы необходимо закрыть файл командой close()
d. После завершения работы файл закроется автоматически

Вопрос 123:
a. Преобразует объект в json-строку
b. Преобразует объект в json-строку и записывает её в файл
c. Преобразует строку формата json в объект python
d. Преобразует строку формата json в объект python и записывает его в файл

Вопрос 124:
a. Файл открыт на запись для записи в бинарном виде
b. Файл открыт на чтение json-строки
c. Объект запишется в файл filename
d. Объект запишется в файл wb

Вопрос 125:
a. r"\d\d-\d\d-\d\d\d\d"
b. r"\dd-\dd-\dddd"
c. r"\d2-\d2-\d4"
d. r"\d*2-\d*2-\d*4"

Вопрос 126:
a. Распознавание букв s, n и w соответственно
b. Распознавание пробела, переноса на следующую строку, букв\цифр\_, соответственно
c. Сокращенное обозначения методов str(), name(), write() соответственно
d. Таких токенов не существует

Вопрос 127:
a. "\w+?"
b. "\w+*"
c. ".+?"
d. "\w+lazy"

Вопрос 128:
a. Централизованной системой управления версиями
b. Распределённой системой управления версиями
c. Языком программирования
d. Консольной командой для установки библиотек

Вопрос 129:
a. Вариант 1
b. Вариант 2
c. Вариант 3

Вопрос 130:
a. Хранения данных об истории изменения кода
b. Игнорирования пользователей git
c. Скрытия файлов и папок от системы Git
d. Описания импортированных библиотек

Вопрос 131:
a. True
b. False
c. None

Вопрос 132:
a. Int
b. List
c. Float
d. Str
e. Dict
f. None
g. Set

Вопрос 133:
a. Изменяемые
b. Неизменяемые

Вопрос 134:
a. 6
b. 4
c. Ошибка, данный код не запустится.

Вопрос 135:
a. 1
b. 3
c. None
d. Ошибка, так как функция всегда должна иметь оператор return, чтобы вернуть результат

Вопрос 136:
a. some_func()
b. some_func(1, 2)
c. some_func(b=4, a=3)
d. some_func(b=10)
e. some_func(6, b=10)
f. some_func(b=10, 6)

Вопрос 137:
a. Нельзя
b. Можно. В этом ничего особенного.
c. Можно, но нежелательно. Так как использование изменяемых типов данных в качестве аргументов по умолчанию может привести к побочным последствиям.

Вопрос 138:
a. При одних и тех же входных данных возвращает одни и те же выходные данные (детерминированность)
b. Записанная с соблюдением всех требований PEP8.
c. Функция не модифицирует значения глобальных переменных внутри себя.

Вопрос 139:
a. 1
b. 5
c. Ошибка, так как нельзя чтобы название локальной и глобальной переменной совпадали.

Вопрос 140:
a. 10
b. None
c. Ошибка, так как внутри функций нельзя использовать глобальные переменные.

Вопрос 141:
a. 1
b. None
c. Ошибка, так как из глобальной области видимости нельзя использовать переменные из локальной области видимости функции.

Вопрос 142:
a. ... просто считываются интерпретатором Python и никак более не обрабатываются, но доступны для использования из стороннего кода и в первую очередь рассчитаны для использования статическими анализаторами.
b. ... считываются интерпретатором Python и статически определяют тип переменной на время работы программы. Если попытаться изменить тип переменной, то будет вызвана ошибка.

Вопрос 143:
a. Inf
b. Данный код выполнится с ошибкой
c. Очень общая ошибка :(
d. Делить на ноль нельзя

Вопрос 144:
a. A
b. B
c. C
d. D

Вопрос 145:
a. [5, 7, 9, 10, 12, 15, 19, 20, 22]
b. []
c. [5, 7, 9, 15, 19]
d. [10, 12, 20, 22]

Вопрос 146:
a. [5, 7, 9, 10, 12, 15, 19, 20, 22]
b. []
c. [5, 7, 9, 15, 19]
d. [10, 12, 20, 22]

Вопрос 147:
a. 0
b. [0]
c. "Hello World"
d. []
e. {}
f. None